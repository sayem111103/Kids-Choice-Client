import useTitle from "../../Hooks/useTitle";

const Addtoy = () => {
    useTitle('Add Toy');
    return (
        <div>
            this is toy adding page
        </div>
    );
};

export default Addtoy;